PORT=4000

# Optional: set this to use the real Anthropic API for bill reading, message
# writing and Q&A instead of the offline mock provider. Leave unset to run
# fully offline.
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-5
